export enum MinesweeperCell {
  Closed = '□',
  Mine = '*',
}

export enum MinesweeperCommand {
  Map = 'map',
  New = 'new',
  Open = 'open',
}

export enum MinesweeperStatus {
  Lose = 'You lose',
  Ok = 'OK',
  Win = 'You win',
}
